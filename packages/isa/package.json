{
  "name": "@cpu-viz/isa",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "RV32I instruction definitions, field encodings, and decoder (framework-agnostic). Handoff §7.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  }
}
