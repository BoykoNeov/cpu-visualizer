{
  "name": "@cpu-viz/engine-deep-pipeline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deep in-order pipeline RV32I — seven stages (IF1 IF2 ID EX1 EX2 MEM WB), a uniformly two-cycle execute. Depends only on isa, trace, and engine-common.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@cpu-viz/isa": "*",
    "@cpu-viz/trace": "*",
    "@cpu-viz/engine-common": "*"
  }
}
