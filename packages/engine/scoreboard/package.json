{
  "name": "@cpu-viz/engine-scoreboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CDC 6600-style scoreboard RV32I — IF, Issue, per-FU Read Operands, out-of-order Execute, and a Write-Result stage that stalls on WAR. Heterogeneous functional-unit latencies are intrinsic to the model, not a config knob. Depends only on isa, trace, and engine-common.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@cpu-viz/isa": "*",
    "@cpu-viz/trace": "*",
    "@cpu-viz/engine-common": "*"
  }
}
