{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*"],
  "references": [
    { "path": "../../isa" },
    { "path": "../../trace" },
    // Test-only edges. `assembler` + `common` (toProgramImage) are used only by this engine's
    // tests — production code consumes the pure ProgramImage. `reference` runs the golden model
    // alongside this one in the INV-8 differential suite (hoisted to a shared harness in step 1).
    { "path": "../../assembler" },
    { "path": "../common" },
    { "path": "../reference" }
  ]
}
