{
  "name": "@cpu-viz/engine-common",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared engine-layer helpers used by every microarchitecture (e.g. the AssembledProgram → ProgramImage adapter). Depends only on isa/assembler/trace.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@cpu-viz/isa": "*",
    "@cpu-viz/assembler": "*",
    "@cpu-viz/trace": "*"
  }
}
