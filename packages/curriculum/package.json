{
  "name": "@cpu-viz/curriculum",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lesson format types, runner, and event-anchoring (framework-agnostic). Handoff §13.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@cpu-viz/trace": "*"
  }
}
