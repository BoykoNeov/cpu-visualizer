---
name: m10-ooo-lesson-track
description: "M10 build log (the out-of-order LESSON track): NOT the content-only milestone it looked like - `slowOpLatency` shipped INERT, only cache-on array-walks witness the toggle, and the headline that inverts M8's (the event multiset is INVARIANT under the OoO toggle)."
metadata:
  node_type: memory
  type: project
---

## M10 — the out-of-order LESSON track (relocated from the MEMORY.md index, 2026-07-23)

Plan: `docs/plans/m10-tasks.md`. Read this before touching the OoO lessons or adding a corpus program.

**M10 (the OoO LESSON track, `docs/plans/m10-tasks.md`) STARTED 2026-07-23 — but NOT the content-only milestone it looked like.** The step-0 dump found the M9-pinned "Option B" (`slowOpLatency`) shipped INERT (config field, no engine consumer) and that out-of-order issue changes NOTHING without a latency source — only cache-on array-walks witness the toggle (`array-sum` 71→59; the memory's "60→41" was one wrong config, an M8-style trap). **User pinned scope "Both, sequenced": an ENGINE step wiring `slowOpLatency`, THEN the fullest track (slow-op namesake + cache-miss money shot).** **Step 1 (engine) DONE (`72c63f3`): a designated slow op (`sll`, RV32I stand-in for a multi-cycle FU) issues once, frees the issue port, occupies an unbounded FU N cycles via a new `'executing'` RobState + `fuCyclesRemaining=N-1`, `stageFuAdvance` runs `executeEntry` at COMPLETION (so the slow op must be a pure producer — deferring a branch/halt would defer `ctx.squash`); parity guard = `slowOpLatency` absent/1 is byte-for-byte M9; mutation-checked (disabling the defer leaves INV-8 green, fails 3/4 `slow-op.test.ts`). **THE load-bearing content finding: a straight-line `[slow→dep→indep]` shows the reorder but NO cycle saving — in-order COMMIT gates the tail; the win only compounds in a LOOP** (why array-sum's cache win is real). So the flagship slow-op program (step 3) MUST be a loop (dumped: N=8 86→53, `a0=72`). **Step 0 DONE 2026-07-23 (`bf6ef06`/`1c552c6`/`4108c0d`, 3233 tests). 0(b) — sweep axes:** added TWO `ConfigAxis` rows to `lessons.test.ts` gated on `configurableOutOfOrder` — `outOfOrderIssue` AND `robSize` (both SWEPT because BOTH are shell-reachable: `IssueOrderToggle`+`RobSizeControl`, `useSimulator` setters). **`slowOpLatency` is NOT swept — held per-lesson** (verified via `App.tsx`+`useSimulator.ts`: no control, no setter, not even in the recorded session config; the plan's "reachable per M9" was optimistic — it shipped config-only, consumer landed at step 1). `positionsFor('out-of-order')` = pred(2)×cache(3)×width(2)×issue(2)×rob(2)=**48** (NOT 96 — `configurableForwarding` FALSE, the CDB IS the forward). **⚠ step-3 finding (RESOLVED by step 3b): the lesson-OPENING path in `useSimulator.ts` threaded NO `slowOpLatency`, so a slow-op lesson would silently record N=1 — step 3 added that plumbing (ref-only, reset in free-play).\*\* **0(a) — re-dump (per-event execution order):** FLAGSHIP PINNED (user) = `array-sum` cache-LARGE `outOfOrderIssue:true` (71→59), over the slow-op loop (de-risk: already corpus + cache plumbed; UI coherence: matches the shipped toggle text). Anchors: the counter `alu-op where {op:add,result:4}` (c20→c9) — the pinned `result:268435460` (pointer bump) is NOT program-unique, it also matches the next iter's load-address add (resolves only via `nth:1`, violating the headline). Slow-op-flagship variant DEFERRED (later session). **⚠ BIG FINDING: there is NO miss-under-miss ANYWHERE in the corpus at width 1** — misses always ~30 cyc apart (unit-stride walk, 4-word line) vs the 10-cyc penalty, so two misses never overlap; the M9 `numMshrs` docblock ("array-sum's two loads are the pair") is FALSE. **Step 4 RESOLVED (user): build a NEW miss-under-miss corpus program — DONE this session (see "Step 4" below).** The trap recorded (and AVOIDED exactly as designed): miss-under-miss must be a TOGGLE effect — a naïve independent-loads program overlaps in BOTH positions (non-blocking loads free the issue port either way); the 2nd miss must be GATED behind an in-order stall (array-sum's shape but STRIDE=line-size 16B, the loop-carried `add` the stall the pointer-bump slides past only under OoO). **Step 2 (flagship lesson) DONE 2026-07-23 (`a3baf7a`, 3338 tests).** `content/lessons/work-slides-ahead.json` + new track "The out-of-order machine" in `index.json` — array-sum cache-large `outOfOrderIssue:true` width 1, `forwarding:false` (neutral, dodges the "only names honored knobs" guard despite OoO `configurableForwarding:false`). 3-beat arc: counter slides under the miss (c9 OoO/c20 in-order, load `mem-read value:5` at c17 both) → the reduction ACCELERATES (`result:118` at c30 OoO vs c42 in-order) → same answer fewer cycles (`mem-write value:120`, 71→59, IPC 0.48→0.58). **Middle-beat CORRECTION (advisor, `b889fe9`): the first draft narrated the reduction as "gains nothing / no faster OoO" — FALSE and self-contradicting (oracle showed 118 12 cyc earlier). The reduction is 12 cyc faster and 71−59=12, so its head start IS the whole win. Accurate mechanism: OoO can't compress the dependent chain but DECOUPLES the loads feeding it (pointer bump no longer stuck behind the waiting add → loads stream in on the cache's schedule). NOTE: the misses do NOT overlap (30 cyc apart, step-0's no-miss-under-miss), so it's load-stream decoupling, NOT miss overlap — the advisor's own proposed "misses overlap" phrasing was also wrong. Oracle now pins the CLAIM: `reductionInOrder−reductionOoo === programDelta`; steps 1&2's stated cycles token-checked. The M4-step-4 gap: the sweep is blind to prose, and the first oracle pinned 118's STRUCTURE (unique, ahead) but not the CLAIM.** **CONSTRAINT found: the reorder FLIPS trace-order between toggle positions, so the two reordered instrs CANNOT both be steps (runner needs monotonic anchors per position); all 3 steps are fixed-program-order events, and the load's `mem-read` (c17) sits BETWEEN the counter's two positions so it can only live in the oracle, never as a step.** Oracle is the primary net (`lessons.test.ts` `describe`): records both toggle positions via `record(outOfOrderIssue)` (forces the toggle itself, M8's `record(issueWidth)` pattern), token-checks the closing 71/59/IPC. `session.test.ts` opening loop extended to assert `outOfOrderIssue`/`robSize` (arrives false/4, flagship declares true/16 → leak failable). Opening plumbing already wired at M9 step 5. **Step 3 (the reservation-station slow-op lesson) DONE 2026-07-23 (3 commits `8dd1546`/`98ec045`/`30f68a1`, 3633 tests).** (a) NEW corpus program `content/programs/slow-op-loop.s` — the deferred `[slow→dep→indep]` loop (= `slow-op.test.ts`'s `LOOP`; default config = single-cycle `sll`, a0=6×12=72, INV-8 identical everywhere). Full ripple, every cell HAND-DERIVED (advisor-audited independent): conformance oracle, pipeline `TIMING` (N=30, S_off 26), superscalar `TIMING` w2 (G=21,Q=14,L_off=26,doomed=5,betting −5; the `sll→add` intra-pair RAW makes `sll` a single-issue group each iter — the ONE diff from `sum-loop`), OoO `PINNED`, pairing 44→35. Cross-check that validated it: width-1 closed form 44 = the slow-op test's empirical N=1. (b) `slowOpLatency` plumbing: `LessonOpening.slowOpLatency` + `lessonOpening ?? 1`; `useSimulator` REF-ONLY (no state/field/control — honored not swept/controllable), set in `startLesson`, read in `loadInto`, **RESET to 1 in `select`/`loadEdited`** (no toggle to undo a leak). `session.test.ts` opening loop arrives latency 3, asserts declared latency — the ONLY headless net; ref threading is BROWSER-ONLY. (c) `content/lessons/reservation-station-holds.json` — OoO w1, cache null, `slowOpLatency:8`, `forwarding:false`+`static-not-taken` (both neutral). 3-beat: counter `add:5` (c8 OoO/c15 in-order, `sll` completes c13 both) → 2nd partial `add:24` (woken cycle after 2nd `sll` broadcast, c20/c27) → final `add:72` (72 both, 53 vs 86, IPC 0.57/0.35). `sll result:12` NOT program-unique (repeats) → oracle-only. **⚠ THE net unique to this lesson: `slowOpLatency` unswept ⇒ the 48-position sweep records at latency 1 where the reorder VANISHES (44/44) — CANNOT check anchor order at the latency-8 recording the browser plays; the oracle adds an explicit latency-8 monotonicity+distinct-cycle check + `add24===sll[1]+1` (causal CDB-wakeup pin) + prose token-checks.** **✅ REF THREADING BROWSER-VERIFIED 2026-07-23** (rig `M:/claud_projects/temp/m10-step8/eyeball.mjs`, drove the SHIPPED `vite preview` bundle via CDP): (1) lesson opens → **53** cyc (proof the shell records at latency 8; 44 = broken), (2) flip issue-order→in-order → **86** (latency SURVIVES the flip), (3) free-play `slow-op-loop` on OoO → **44** (`select` reset the ref to 1, leak-guard fires). **Readout finding: the OoO model has NO IPC tile** (`showIssue` needs `micro.width`, which OoO omits — that's the `MicroTablePanel` "no width field" note paying off) — the computed cycle count is the **scrub bar's `max`+1** (`recordedCycles−1`), NOT `PairingReadout`; the pipeline map shows the `sll`'s wide ~8-cyc ROB band, counter/branch issuing around it (OoO) vs serialized (in-order). **Step 5 (the in-order-commit lesson) DONE 2026-07-23 (`48fa8dc`, 3739 tests).** `content/lessons/commit-in-order.json` — the ROB's precise-state job on the SAME array-sum machine as the flagship (cache-large, OoO, width 1), seen through the COMMIT stream not the execute stream. **⚠ SCHEMA FINDING (advisor): the plan's step-5 anchor plan ("anchor on an `instr-retire where`") was NOT achievable** — `instr-retire` carries ONLY `{instr}`, and `instr` is the fetch-order id (`i0`/`i4`/`i7`…, `id:\`i${seq++}\``at`processor.ts:1303`), an `nth`-in-disguise the M10 headline forbids. So steps anchor on program-unique alu-op VALUES (iter-2 counter `result:3`c16, iter-3 sum`result:18`c22, iter-5 sum`result:120`c48 — an alu-op so distinct from the flagship's`mem-write value:120`, all differentiated from the flagship's 4/118/120 per the advisor), and the retire-order claim lives in the ORACLE via the alu-op→id→instr-retire join (id stable within one recording, INV-4). **THE framing trap the advisor caught (correctness): "retire in program order" is true in BOTH toggle positions** (ROB always commits in order AND in-order issue never scrambled execution — the dependent `add` blocks the independents from issuing), so the lesson is NOT "both retire in order" (makes the toggle look INERT, the flagship's old middle-beat shape). It IS: **OoO SCRAMBLES finishing order, the ROB puts it back; in-order never scrambled.** Oracle discriminator: in-order counter finishes AFTER the load (`aluCycle(inOrder,add,4)===20 > memRead(5)===17`, no scramble) vs OoO (9 < 17), plus a strictly-ascending-retired-id stream in BOTH positions (parsed from the `iN`ids) and the execute-early/retire-late pair (counter execs c9 < load c17, retires c22 > load c18). Cycle-count numbers (59/71) kept in the EXPERT tier since the subject is ORDER, not speed. **⚠ PROGRAM DEVIATION (advisor): "rides step 3/4's program" was stale** — step 4 deferred left only slow-op-loop (RS's program, pinned LAST), which would introduce the slow shift before the lesson explaining it; used array-sum (understood cold from the flagship). **Teaching-order TODO from step 3 — DONE:`reservation-station-holds`MOVED to the OoO track END in`index.json`; track is now `[work-slides-ahead, commit-in-order, reservation-station-holds]`** (renaming inserts at position 2 if it lands). Wiring: `LESSONS.length`17→18, cache-canonicalization id list (commit-in-order is the 5th cache-declaring lesson), OoO by-name membership set. **Step 6 (renaming, "A new name for a register") DROPPED 2026-07-23 WITH PROOF (advisor-endorsed) — the OoO track is FINAL at 3 lessons `[work-slides-ahead, commit-in-order, reservation-station-holds]`, no `index.json` change (renaming was never wired).** Ran a throwaway rename dump (`zz-m10-rename-dump.test.ts`, deleted; `M:/claud_projects/temp/m10-renaming/`) scanning the corpus for "two live ROB entries share the same `rd`" (one arch reg, two in-flight tags = the renaming payoff). **The structural finding that meets the plan's own drop criterion with proof: renaming's payoff is unobservable without a latency source to stall the OLDER same-register writer, and the only two sources (cache miss / slow op) each make THEMSELVES the salient cause — so renaming is ALWAYS the hidden enabler behind a louder cause, never the visible star.** The ONLY two independent same-reg reorders in the whole corpus: `array-sum` `t2`(cache salient) and`slow-op-loop` `t3` (slow-op salient AND it's the RS lesson's program, off-limits per teaching order). Every other two-tag case (`a0`/`t0`/`t1`) is a loop-carried TRUE dep, nothing reorders. **There is NO honest toggle counterfactual:** the array-sum `t2` reorder (`mem-read value:17`HIT fires c14 BEFORE`value:5`MISS c17 under OoO; under in-order value:5 first c17, value:17 c25, and`t2`NEVER appears in two tags) is caused by the ISSUE POLICY + the front-end freeze, NOT renaming (identical in both toggle positions). Under in-order a miss sets`ctx.memStall`which freezes BOTH`stageDispatch` (`processor.ts:1153`) and `stageIssueExecute` (`:836`) — the 2nd `lw t2`is never even DISPATCHED, so the 2nd tag never exists (renaming isn't "unused" in-order — the freeze prevents the 2nd dispatch). A toggle-A/B renaming lesson would just RE-TELL the flagship with a load standing in for the counter. Dropped, no`rename`/`issue`/`commit`event invented (the milestone's characteristic-failure temptation declined with proof, as M8 step 8 declined`issue`). **Step 4 (the cache-miss money shot "Racing ahead of the miss") DONE 2026-07-23 (2 commits `28ba482`corpus+ripple,`f060eeb`lesson+oracle, 4035 tests) — the deferred miss-under-miss program was built and the beat landed.** (a) NEW corpus program`content/programs/strided-sum.s` = **`array-sum`'s TWIN**: byte-for-byte the same instruction stream, the ONLY source changes being the pointer bump (`addi t0,t0,16`vs`,4`) and distinct `.data`values (7,20,−3,50,6 → a0=80; framed as five 16-byte records, one summed field each). Stride = one LINE ⇒ every load a fresh block ⇒ misses, and the`sw`to`total`misses a 6th block ⇒ **6 misses at BOTH sizes** (all compulsory, nothing re-read, cache size buys nothing). **That twin-ness made the whole INV-8 ripple near-mechanical: every cell is array-sum's own entry with ONLY`misses`2→6** (conformance; pipeline`TIMING`; superscalar `TIMING`w2 — the partition is dependency-shaped and CACHE-BLIND so groups/pairs/blocked/doomed/betting are IDENTICAL; OoO`PINNED`; pairing `{w1:51,w2:42}`= array-sum's because that table runs cache-OFF). All four tables first-run green. **⚠ THE one cell that was NOT "array-sum + count" (advisor caught it BEFORE it was written): array-sum's store HITS, this one MISSES, and NO existing test pinned whether the M3/M7 engines charge`missPenalty`for a no-write-allocate store miss** — the difference between`{6,6}`(M=60) and`{5,5}`(M=50). Verified by running the pipeline AND superscalar (w1+w2) engines directly first: cache-off byte-identical to array-sum (72 off/51 on), every extra miss +10 cyc (pipe on/large 111=71+40, off/large 132=92+40; SS w2 on/large 102=62+40). The store miss IS charged by all three. (b) **Toggle effect 111→62 (−49), the BIGGEST in the collection** (flagship is −12). Overlap confirmed by counting`awaitingMem`ROB entries/cycle: OoO overlaps at [13–16,20–23,27–30,34–37] (max 2 = MSHR count); **in-order NEVER has >1 outstanding miss** — the 2nd miss is gated behind the`memStall`front-end freeze. (c)`content/lessons/racing-ahead-of-the-miss.json`at OoO track **position 2**; 3 beats all program-unique`where`in strict PROGRAM order across iterations, so **unlike the flagship NO reorder can flip them** (that constraint never binds):`mem-read value:7`(c17 BOTH) →`mem-read value:20`(c24 OoO/c35 in-order) →`mem-write value:80`(62 vs 111, IPC 0.55/0.31). The miss-under-miss itself is a`cache-access`c14 vs c25 — ORACLE-only (its sole program-unique field is a raw address). **THE distinctness (advisor): the flagship hides independent ARITHMETIC in ONE miss's shadow and its misses NEVER overlap (step 0's finding); this aims the same lever at the ADDRESS chain so what slides into the shadow is another MISS — real MLP, MSHR-bounded at 2.** Without that framing the two lessons read as the same lesson twice. Oracle proves the overlap **TWO independent ways** (cycle-ordering AND load/store-unit co-residency — a cycle argument alone could be satisfied by a re-detected single miss) + an **unchanged-penalty** check (release−detect equal both positions ⇒ "overlapped" not "shortened"; a faster cache wouldn't satisfy it) + bigger-win-than-flagship (pins 71/59 too). **⚠ This lesson's EXTRA sweep blindness (differs from RS's):`cache`IS a swept axis, so`positionsFor`records it at cache-OFF where the program has NO misses, both positions run 51 cycles, and NOTHING the prose says is true — yet anchors still resolve in order (program-order-separated), so the sweep stays green over a machine the prose doesn't describe. Oracle pins cache-off 51===51 explicitly.** No shell wiring needed:`EXAMPLE_PROGRAMS`globs`content/programs/\*.s`. **Step 7 (wire) DONE 2026-07-23 with NO code change — an AUDIT, not an edit.** Every acceptance item was already pinned by a named test and step 7's job was to say WHICH: membership `lessons.test.ts:669`, track name `:584`+`:706`, `lessonSections` totality/no-`UNTRACKED_HEADING` `:701`(this step's acceptance verbatim), full order`:517`. **⚠ THE step-7 finding — a full-sequence teaching-order test was DECLINED with reason (advisor-endorsed): the cache track's own comment states the discriminator ("forced by the prose, not by taste"), i.e. an order pin earns its place ONLY if a prose sentence LIES when reordered.** flagship→racing lies (racing says "the previous lesson") and is already pinned at `:3202`, which also locks flagship=0/racing=1; commit-in-order and reservation-station-holds back-reference NEITHER each other nor anything (RS's ROB sentence is self-contained), so `flagship<racing<commit<RS`would assert taste as law and redden on a lawful reorder. Precedent: **M8's 4-lesson wide track shipped membership + per-lesson describes and NO sequence test.** Also re-verified: zero`nth`in all four OoO lesson files; gate green at **4036 tests** + typecheck/lint/format/build. **Step 8 (browser pass) DONE 2026-07-24 — M10 COMPLETE. Drove the SHIPPED`vite preview`bundle over the whole 4-lesson track via CDP (rig`M:/claud_projects/temp/m10-browser/eyeball.mjs`); ALL 33 checks PASS, every screenshot coherent, no M10 defect.** Confirmed: picker shows "The out-of-order machine" track with its 4 lessons in teaching order (read off the OoO `<optgroup>`); each opens `model=out-of-order` with the OoO issue-order button pressed (`aria-pressed` scoped to the exact button text — M8 first-`.seg-btn`trap dodged) and RECORDS at its declared config — per-lesson total (scrub max+1) reads the OoO value **59/62/59/53**, never the in-order **71/111/71/86** (the M8 wrong-trace trap ruled out live, incl. the RS lesson's`slowOpLatency:8` → 53 not 44). Two traps handled: (1) **`commit-in-order`'s anchor/prose gap** — its steps anchor iteration-2+ (cursor 16/22/48) while the prose narrates iteration-1 (c9/c17), so matched cursor against the KNOWN anchor cycle, NOT a scraped number; (2) **narration read from the VISIBLE `<p>`only** — the panel stacks every step's`<p>`at`gridArea 1/1`with only the active one`visibility:visible`, so a body-text search would match a hidden ghost. Screenshots READ, not just DOM: flagship ROB shows the stalled load executing + dependent add waiting on `⤺ROB#tag`+ counter completed=4;`commit-in-order`shows the **completed-but-not-retired pile** (ROB#9–15 completed, head committing — the lesson's whole point); RS shows the`sll` holding its FU 8 cycles. Toggle discriminator live: flagship 59→71, racing 62→111. **⚠ ONE observation, NOT a defect, correctly NOT fixed: narration uses ABI reg names (`lw t2`, `sll t3`, `addi t1`) while the disassembler renders x-names (`lw x7`, `sll x28`, `addi x6`) — the SAME register under two names, and the ESTABLISHED app-wide convention (M1–M8 lessons narrate the same way: `forwarding-bubble`/`cache-spatial`/`two-at-once`). NOT the auipc/lui wrong-mnemonic class the browser memory warns about; rewording only the M10 four would make them inconsistent with every other track. Changing it is a global product decision, out of M10's scope.** **M10 HEADLINE (inverted M8): the event multiset is INVARIANT under the toggle** — no anchor discriminates it, so narration ORACLES are primary and anchors use program-unique`where`not`nth`; renaming PROVED un-anchorable (dropped, no event invented).
