# Project memory — CPU Visualizer

- [M9+M10 review resolved](m9-m10-review-resolved.md) — **✅ all 10 findings FIXED 2026-07-24** (repo 4036→4051 tests, F2/F4 browser-verified); holds the bug-class guardrails for expanding the OoO corpus/models/knobs. Ranked write-up at `docs/reviews/m9-m10-review-findings.md`.
- [Project overview](project-overview.md) — what it is, stack, and the full milestone log. **M1–M10 COMPLETE.** **M9 (out-of-order/Tomasulo/ROB/renaming, `docs/plans/m9-tasks.md`) — the north-star tier, "the genuine cliff" — is DONE: all steps 0/1a/1b/2/3/4/5/6/7 landed 2026-07-22..23, all decisions pinned 2026-07-21.** Engine (config seam → in-order-issue base → the OoO scheduler → INV-8 differential → the per-instruction lifecycle table → recorder/`follow()` through the ROB, INV-4 payoff) → web enablement (the flagship in-order↔out-of-order toggle + ROB-size control, flip drops `array-sum` 60→41 live) → the tier's STAR SURFACE, the `MicroTablePanel` (ROB/RS/rename tables folding over `MachineState.micro`; per-ENTRY ROB copy dodges the signature time-travel aliasing bug; gate has NO `width` field so it can't collide with `PairingReadout`) → **the bespoke OoO datapath (step 7, the sheddable half that never had to be shed): `datapath-out-of-order.ts` — PC→instr-mem→decode/rename dispatching into the ROB + reservation stations, which issue to a functional-unit pool + load/store unit whose results ride the Common Data Bus back to RS+ROB, ROB head committing in order.** The ONE load-bearing call: it's the ONLY datapath whose `activate` folds `state.micro` (box occupancy — an OoO `location` is uniformly `"ROB#tag"`, no stage) AND `events` (flow); coherence of that pairing was DUMPED on `array-sum` around the first miss before any geometry (the superscalar's "NEVER `micro`" warning does NOT apply — OoO `micro` is the cursor's own state, not a cycle ahead). Structural not per-lane (ROB/RS/FU are POOLS); wire=region hue, box=hue-neutral pool, follow-ring=identity on wires (renderer class `dp-follow`, NOT the tables' `dp--follow`). Browser-verified by reading the LIVE SVG: at `array-sum` cycle 16, 8 wires light with 4 distinct region hues matching the dump; following ROB#5 rings 4 datapath wires + its table row. Full step-by-step log in project-overview.md's **"M9"** section — read it before touching the out-of-order package; the condensed M2/M6/M7/M8 log lives in that file's **"Condensed milestone log"** section. Repo now **3233 tests**, typecheck/lint/build/format:check green. **M10 (the OoO LESSON track, `docs/plans/m10-tasks.md`) ✅ COMPLETE 2026-07-24 — all steps 0–8 done, step 6 (renaming) DROPPED with proof.** **Step 8 (browser) drove the SHIPPED bundle over the whole 4-lesson track via CDP (rig `M:/claud_projects/temp/m10-browser/eyeball.mjs`): ALL 33 checks PASS, every screenshot coherent, no M10 defect.** Confirmed the picker track + order, each lesson opens on `out-of-order` with the OoO button pressed (`aria-pressed` scoped — not the first `.seg-btn`) and RECORDS at its config (totals 59/62/59/53, never in-order 71/111/71/86 — RS's `slowOpLatency:8` → 53); handled `commit-in-order`'s anchor/prose gap (cursor vs KNOWN anchor cycle, not scraped prose) and the stacked-invisible narration `<p>` (read the visible one only); toggle discriminator live (flagship 59→71, racing 62→111). **ONE observation NOT a defect (correctly unfixed): narration uses ABI reg names while the disassembler renders x-names — the ESTABLISHED app-wide convention (M1–M8 narrate the same way), same register two names, out of M10's scope.** Step 7 needed NO code — an audit naming the test that pins each acceptance item, which DECLINED a full-sequence teaching-order test on the cache track's own rule ("a pin earns its place only if a prose sentence lies when reordered"; M8's 4-lesson wide track has no sequence test either). Repo **4036 tests** green. NOT the content-only milestone it looked like: step 0 found the M9-pinned `slowOpLatency` shipped INERT, so M10 led with an ENGINE step. The track is FINAL at four lessons — `[work-slides-ahead, racing-ahead-of-the-miss, commit-in-order, reservation-station-holds]` — and TWO corpus programs were added (`slow-op-loop.s`, `strided-sum.s`), each paying the full INV-8 ripple. **M10 HEADLINE (inverted M8): the event multiset is INVARIANT under the `outOfOrderIssue` toggle** — no anchor discriminates it, so narration ORACLES are the primary net and anchors use program-unique `where`, never `nth`; renaming was PROVED un-anchorable and dropped with no event invented. **Full step-by-step log, every finding and trap, in project-overview.md's "M10" section — read it before authoring an OoO lesson or widening the corpus.** A `/code-review high` over M9+M10 found 10 findings, **all FIXED 2026-07-24** (repo now **4051 tests**) — see [[m9-m10-review-resolved]] for the bug-class guardrails before expanding the OoO corpus, models, or knobs.
- [M11 deep pipeline — steps 0–5 done, THE NET landed, the map criterion PAID OUT, and the model is DRIVABLE](m11-deep-pipeline-planned.md) — **the spec §12 roadmap is FINISHED (M1–M10)**, so next work comes from the don't-foreclose flag. M11 = the 7-stage deep pipeline; **steps 0 (scaffold + DAG ripple), 1 (the model MVP — it runs), 2 (INV-8 differential — 6 configs, 68 tests, green first run), 3 (THE NET — the timing matrix, 92 tests, repo 4140→4232), 4 (recorder/time-travel + the map meets a real deep engine, 19 tests, repo 4232→4251) and 5 (web enablement — the picker row, 14 tests, repo 4251→4265) DONE 2026-07-27; step 6 (cache, or DROPPED WITH PROOF) is next, steps 6–8 open** (`docs/plans/m11-tasks.md`), scope pinned to depth ALONE. **STEP 5's two findings, both generalizing beyond this model: (a) the row made a LIVE CRASH reachable — the shell hands every engine the whole SESSION config, which was safe only because every model IGNORED an unhonored knob, and this is the first engine that REFUSES one (a non-null cache throws), so `engineConfigFor` in `web/src/models.ts` now clamps the cache for a model declaring `configurableCache: false`; clamping is FORCED, not taste, because the cache CONTROL is gated on that same flag, so an error message would strand the user with no control to escape by. It clamps the value PASSED, never the session's, and `cache` ALONE. (b) the browser found the milestone's only defect and it was PROSE — the prediction tooltip stated the 5-stage's "a correct bet costs 1 cycle, a wrong one costs 2" on a machine where they are 2 and 4 (INV-5 contradiction, not simplification), reworded to name the MECHANISM rather than threading coefficients through `ModelChoice` or the trace. Ask of every new model: what user-visible prose is gated on a flag it turns on?** Also: `useSimulator` holds the whole `ModelChoice` in ONE ref now (two refs × three assignment sites is how the LESSON path stays broken while the picker looks fixed); the churn was FOUR exhaustive `toEqual` lists, not three (the DATAPATH table too); and the vite-alias question is settled EMPIRICALLY — the alias resolves to SOURCE and a live edit lands on reload, while no-HMR-without-reload is identical on `engine/pipeline`, hence pre-existing, not this package's doing. **STEP 4 PAID OUT the first falsifiable UNCHANGED criterion — `pipeline-map.ts` absorbed a real seven-stage recording untouched, so the M3-era hand-built fixture is now emitted by an engine — and it CORRECTED acceptance criterion 2, which named the wrong stage: the ALU→ALU bubble draws a repeated `ID` cell, NOT EX1** (the interlock re-presents its occupant onto the latch it arrived on, so `add.s` walks `IF1 IF2 ID ID EX1 EX2 MEM WB` at 10 cycles vs the 5-stage's `IF ID EX MEM WB` at 7 — **step 5's browser pass must look at the ID column**). Step 4 also took the **web trio** step 0 had deferred (dep/`paths`/vite alias — three different gates check them, so they land together, and step 1's "step 4 may have to move after step 5" is resolved: it did not), stated the map-side flush assertion falsifiably (names flushed === rows killed), and pinned "seven in flight" WITH its honest negative — `byte-loads` and `paired-branches` hold **5 on BOTH** machines and `sum-loop` peaks at **6**, so use `array-sum` for a genuinely full pipe and never assert `deep > five` corpus-wide. **STEP 3's headline: the closed form is `cycles = N + 6 + S + P` where S is stalls on the RETIRED path — because it FOUND that `S` is NOT prediction-invariant.** Resolving at end-of-EX2 gives a taken transfer's fall-through a whole LIVE cycle in ID, so it stalls and is then squashed — a stall costing zero cycles; the 5-stage cannot do this (it resolves a stage earlier, so its squash early-return fires first). So **two of the 5-stage's assertions do NOT port** (`S untouched by prediction`, and `casualties ARE the penalty` — `sum-loop` kills 18 while paying P=36), **five distinct flush shapes exist and the shape depends on the FORWARDING toggle too**, and the `+6` constant needed a hand-built program because **the corpus has no dependency-free program left — depth took it away**. Mutation check run as **TWO** mutations, INV-8 green 68/68 under both while timing went RED 55/92 and 58/92 (stubbing EX2 also requires dropping the `ex-latency` arm, or it's a half-mutation); **step 2's reusable bits: the cache axis is absent BY REFUSAL not omission (restoring it throws, it does not redden), `cache: null` is written explicitly rather than inherited, and `deep-pipeline → engine-conformance` was CONFIRMED-BY-RUNNING to lint clean — the step-0 deny path nobody had exercised.** leads with the timing matrix because INV-8 cannot see a 5-stage wearing seven labels. **ALL decisions pinned 2026-07-27** — Option A / EX2 a real uniformly-2-cycle ALU / every control transfer resolves at end of EX2. **Step 1 CONFIRMED every PER-HAZARD coefficient on the first run** (not step 3's corpus-wide closed form — that is still the net), including the flagged trap: under prediction=ON a misprediction arrives as **two width-2 flushes**, and a correctly predicted taken branch costs **2**. It also found a THIRD flush shape the plan never named — a `jalr` correcting over a younger bet emits a **non-contiguous `['EX1','IF1']`**, so read the penalty as a TOTAL, never a shape. Its shaping call: **EX1 is the forwarding network ALONE and `Ex1Ex2Latch` carries OPERANDS, not a result** — so the ALU→ALU bubble is structural, not a rule. Step 0's finding: the eslint `deny()` guardrail has THREE paths and the plan named one — **verify a new model in both directions, incl. the new package importing its parent**, which is what step 1's fork would reach for.
- [Future microarchitectures](future-microarchitectures.md) — user wants **deeper pipelines + superscalar** later; a don't-foreclose flag, NOT build-for-it-now. Only the pipeline map must stay stage-and-lane-parametric. **Depth is now M11 ([[m11-deep-pipeline-planned]]); width is still unscheduled.**
- [Workflow rituals](workflow-rituals.md) — batch-end / "session end" = update memory+docs, commit, push.
- [Best-practices source](best-practices-source.md) — the guide the user asked to apply, and what was adopted/skipped.
- [Commit and push preference](feedback_commit_and_push.md) — always commit and push after every change, no confirmation needed.
- [The browser is the only net](browser-is-the-only-net.md) — this repo's headless tests are `renderToStaticMarkup` with no jsdom, so **no test can see a click**; 9 of 10 view steps shipped a defect only the browser caught (step 5 was the first exception). How to drive the real app here. **Corrected 2026-07-17:** `chrome.kill()` does NOT kill the browser (21 leftovers; the next run inherits the last one's page) — use `taskkill /PID <pid> /T`; and a **debug port** can belong to a stranger's Chrome, so never let the target-finder fall back to "any page". **Added 2026-07-27 (M11 step 5, where 5 of 5 "failures" were the RIG): assert the NEGATIVE state first — a selector that can only ever return "visible" passes the positive check vacuously; the dev server's cold first paint is ~18s (poll for a minute, not ten seconds); and sweep leftover Chromes by matching your `--user-data-dir` in `CommandLine`, never by image name (66 leftovers this time).**
- [Never kill dev servers by port](never-kill-dev-servers-by-port.md) — the user runs **several vite projects** that climb past each other on 5173+ (Black Hole Lab / CPU Visualizer / Storm Diorama seen live on 5173/5174/5175); **a port never tells you whose server it is** — identify by served `<title>`. A prior session killed 19 servers by port and destroyed live work. **Applies to CDP debug ports too** — a fixed one attached a drive to the user's "Physical Synthesis — viewer" tab (M5 step 5).
